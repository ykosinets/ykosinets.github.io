# TorqueLink Media static site

Edit sectioned CSS in `src/css/`. The browser still loads `styles.css`, which is generated from `src/css/main.css`.

## Setup

```sh
npm install
```

## Common commands

```sh
npm run build
npm run watch:css
npm run serve
npm run lint:css
npm run format
```
